#!/usr/bin/env bash

set -euo pipefail

command -v ctags >/dev/null 2>&1 || {
  echo "git-ctags: Universal Ctags is required; install the core Brewfile first" >&2
  exit 127
}

git rev-parse --git-dir >/dev/null
git ls-files --deduplicate | ctags "$@" -L -

#!/bin/sh

set -eu

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
remote=${GIT_UP_REMOTE:-origin}

git fetch "$remote"
branch=$("$script_dir/git-default-branch" "$remote")
git rebase "$remote/$branch" "$@"
